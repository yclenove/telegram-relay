# Cursor 编辑器本地目录，避免未跟踪文件干扰 git 状态
.cursor/
/relay
*.exe
*.log
.env
configs/config.private.yaml
.env.local
secrets/
configs/private/config.local.yaml
configs/private/*.secret.yaml

# 本地构建与打包输出（由 go build / pack-deploy-bundle 生成）
build/
release/
